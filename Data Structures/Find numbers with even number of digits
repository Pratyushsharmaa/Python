class Solution(object):
    def findNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        result = []
        for num in nums:
            if len(str(num))%2==0:
                result.append(num)

        return len(result)

        
